@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<Course>
<div class="card-body">
	<!-- Modal starts -->
	<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" style="display: none;" aria-hidden="true">
		<div class="modal-dialog modal-lg" role="document">
			<div class="modal-content">
				<div class="modal-header">
					<h5 class="modal-title" id="exampleModalLabel">Delete</h5>
					<button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
						<span aria-hidden="true">×</span>
					</button>
				</div>
				<div class="modal-body">
					<p>Are you sure you want to delete this?</p>
				</div>
				<div class="modal-footer">
					<a id="confirmDeleteBtn" role="button" href="/Course/Delete" class="btn btn-danger">Submit</a>
					<button type="button" class="btn btn-light" data-bs-dismiss="modal">Cancel</button>
				</div>
			</div>
		</div>
	</div>
	<!-- Modal Ends -->
</div>

<input type="text" id="searchBox" placeholder="Search..." class="form-control">
<div id="searchResults"></div>

<a role="button" asp-controller="Course" asp-action="Add" class="btn btn-success">New</a>

<div class="card">
	<div class="card-body">
		<div class="table-responsive">
			<table class="table table-striped">
				<thead>
					<tr>
						<th scope="col">ID</th>
						<th scope="col">Name</th>
						<th scope="col">Topic</th>
						<th scope="col">Minimum Degree</th>
						<th scope="col"></th>
						<th scope="col"></th>
					</tr>
				</thead>
				<tbody>
					@if (Model != null)
					{
						@for (int i = 0; i < Model.Count; i++)
						{
							<tr>
								<td>@Model[i].CourseId</td>
								<td>@Model[i].name</td>
								<td>@Model[i].topic</td>
								<td>@Model[i].MinimumDegree</td>
								<td>
									<a role="button" href="/Course/Edit/@Model[i].CourseId" class="btn btn-warning">Edit</a>
								</td>
								<td>
									<button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#exampleModal" data-id="@Model[i].CourseId">Delete</button>
								</td>
							</tr>
						}
					}
				</tbody>
			</table>
		</div>
	</div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
	$(document).ready(function () {
		$("#searchBox").on("keyup", function () {
			let query = $(this).val();
			if (query.length > 1) {
				$.ajax({
					url: '@Url.Action("Search", "Student")',
					type: "GET",
					data: { searchTerm: query },
					success: function (data) {
						$("#searchResults").html(data);
					}
				});
			} else {
				$("#searchResults").empty();
			}
		});
	});
</script>

<script>
		document.addEventListener("DOMContentLoaded", function () {
		var deleteButtons = document.querySelectorAll("button[data-bs-toggle='modal']");
		var confirmDeleteBtn = document.getElementById("confirmDeleteBtn");

		deleteButtons.forEach(function (button) {
			button.addEventListener("click", function () {
				var CrsId = button.getAttribute("data-id");
				confirmDeleteBtn.href = "/Course/Delete/" + CrsId;
			});
		});
	});
</script>

@section Scripts
{
	<script src="~/assets/js/modal-demo.js"></script>
}